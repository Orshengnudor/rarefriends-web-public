/* The two control scales shared by the primitives. Per-component contracts live in components/. */

export type Size = "sm" | "md" | "lg";

export type Variant = "secondary" | "primary" | "signal" | "ghost";
